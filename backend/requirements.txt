# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Config / env
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# File parsing
PyMuPDF==1.25.1          # PDF text extraction (fitz)
python-docx==1.1.2       # DOCX
python-pptx==1.0.2       # PPTX

# OCR (fallback for scanned PDFs / images)
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==11.1.0

# URL scraping (GitHub / portfolio links) — Phase 3
# No PyGithub: it issues its own HTTP, which would route GitHub API calls
# around ingestion/url_guard.py and make "every outbound fetch goes through
# safe_get" no longer checkable. github_scraper.py uses the REST API directly.
requests==2.32.3
beautifulsoup4==4.12.3

# LLM (Gemini 3 Flash, free tier) — used from Phase 2 onward
google-generativeai==0.8.3

# Testing
pytest==8.3.4
httpx==0.28.1            # required by fastapi.testclient.TestClient
